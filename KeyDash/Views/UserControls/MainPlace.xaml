<UserControl x:Class="KeyDash.Views.UserControls.MainPlace"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KeyDash.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="800"
            >
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height ="10"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="1" Name="TextBoxMainText" Grid.Column="1" HorizontalAlignment="Center" MaxHeight="400" 
            TextAlignment="Justify" FontSize="50"   Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" TextWrapping="WrapWithOverflow" />
        <Line VerticalAlignment="Center" HorizontalAlignment="Center" Fill="Transparent" Grid.Row="0" Stroke="Black"  
              StrokeThickness= "2" X1="0" Y1="0" X2="500" Y2="0"/>
        <Button Name="BtnStart" Grid.Row="2" Height="70"  Background="Transparent" BorderThickness="0" FontSize="35" Focusable="False" Content="Start" Click="BtnStart_Click" />
        <local:TimerUi Grid.Row="1" x:Name="Timers"/>
    </Grid>
</UserControl>
