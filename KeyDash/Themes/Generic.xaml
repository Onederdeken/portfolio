<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:KeyDash.Controls">
    
    <SolidColorBrush x:Key="ControlBackground" Color="#402A2A2A"/>
    <SolidColorBrush x:Key="ControlBorder" Color="#50444444"/>
    <SolidColorBrush x:Key="ControlHoverBckground" Color="#60353535"/>
    <SolidColorBrush x:Key="AccentColor" Color="#88FFFFFF"/>
    <SolidColorBrush x:Key="TextForeground" Color="#E1E1E1"/>
    <CornerRadius x:Key="BorderRadius">8</CornerRadius>
    <CornerRadius x:Key="BorderRadiusLeft">8,0,0,8</CornerRadius>
    <CornerRadius x:Key="BorderRadiusRight">0,8,8,0</CornerRadius>
    <Style TargetType="{x:Type local:NumericUpDown}">
        <Setter Property="FontSize" Value="25"/>
        <Setter Property="Template">
            <Setter.Value >
                <ControlTemplate TargetType="{x:Type local:NumericUpDown}">
                    <ControlTemplate.Resources>
                        <Style TargetType="RepeatButton">
                            <Setter Property="Background" Value="{StaticResource ControlBackground}"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="BorderBrush" Value="{StaticResource ControlBorder}"/>
                            <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="RepeatButton">
                                        <Border x:Name="CustomRepeatBtnBorder" Background="{TemplateBinding Background}" CornerRadius="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="CustomRepeatBtnBorder" Property="Background" Value="{StaticResource ControlHoverBckground}"/>
                                                <Setter Property="Foreground" Value="#00F2FF"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="CustomRepeatBtnBorder" Property="Background"  Value="#30FFFFFF"/>
                                                <Setter TargetName="CustomRepeatBtnBorder" Property="RenderTransform">
                                                    <Setter.Value>
                                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                                    </Setter.Value>
                                                </Setter>
                                                
                                            </Trigger>
                                            
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                    
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ControlTemplate.Resources>
                    <Border BorderBrush="{StaticResource ControlBorder}" CornerRadius="{TemplateBinding CornerRadius }"  BorderThickness="1">
                        <Grid  >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="60*"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <RepeatButton Name="PART_DownButton" BorderThickness="0"   Height="auto"  Grid.Column="0" Content="&lt;"  />
                            <TextBox x:Name="PART_text" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" BorderThickness="0"  Padding="10,0,10,0" 
                                     Foreground="{StaticResource TextForeground }"  
                                     BorderBrush="{StaticResource ControlBorder}"
                                     Background="{StaticResource ControlBackground}" Height="auto" MinWidth="50" Grid.Column="1" Text="{Binding Value, RelativeSource={RelativeSource TemplatedParent}}" />
                            <RepeatButton x:Name="PART_UpButton" BorderThickness="0"  Height="auto" Grid.Column="2" Content=">" />
                        </Grid>
                    </Border>
                    
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
